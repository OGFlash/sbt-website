# Frontend environment variables
# Vite exposes vars prefixed with VITE_ to the browser bundle.

# Backend URL for Vite's dev proxy — use service name when running in Docker
BACKEND_URL=http://backend:8000
